import { PersistentPanelDirective } from './persistent-panel.directive';

describe('PersistentPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new PersistentPanelDirective();
    expect(directive).toBeTruthy();
  });
});
